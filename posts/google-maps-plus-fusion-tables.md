# Google Maps + Fusion Tables

Coming soon. ;-)

JS example:

    #javascript
    if (this === "javascript") {
        syntax_highlighted();
    }

CoffeeScript example:

    #coffeescript
    if this is "coffeescript"
      syntax_highlighted()
    else
      alert "bummer"
