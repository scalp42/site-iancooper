# Google Maps + Fusion Tables

Coming soon. ;-)

JS example:

    #js
    if (this === "javascript") {
        syntax_highlighted();
    }

CoffeeScript example:

    #coffee
    if this is "coffeescript"
      syntax_highlighted()
    else
      alert "bummer"
